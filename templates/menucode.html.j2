{% for page in pages %}
    <li class="nav-item">
        {%if page.title == page_name %}
            <a class="nav-link current" href="{{page.doc_name }}"> {{page.title| title}} </a>
        {% else %}
            <a class="nav-link" href="{{page.doc_name }}"> {{page.title| title}} </a>
        {% endif %}
    </li>
{% endfor%}
